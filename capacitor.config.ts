import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.gmap',
  appName: 'gmaps-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
